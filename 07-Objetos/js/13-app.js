const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300, 
    disponible: true,
}

//Nos devuelve las claves del objeto
console.log(Object.keys(producto));

//Nos devuelve los valores del objeto
console.log(Object.values(producto));

//Nos devuelve todo el objeto en pares (Clave y valor)
console.log(Object.entries(producto));

